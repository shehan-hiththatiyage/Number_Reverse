# Number Reverser

This program reverses a given integer number.

### Example
Input: 1234  
Output: 4321

### How it works
- Takes the last digit using `% 10`
- Builds the reverse number step by step
- Uses a `while` loop

### Compile & Run
```bash
gcc reverse_number.c -o reverse
./reverse
Simple C program to reverse a number
